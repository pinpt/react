import { create } from '@storybook/theming';

import logo from './logo.svg';

export default create({
	base: 'light',
	brandTitle: 'Pinpoint React',
	brandUrl: 'https://pinpoint.com',
	brandImage: logo,
});
